{
    "functions": {
        "api/app.py": {
            "memory": 1024,
            "maxDuration": 10
        }
    },
    "buildCommand": "cd api;flask run -p 3000",
    "installCommand": "pip install -r requirements.txt",
    "outputDirectory": "./api/templates"
}