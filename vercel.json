{
    "functions": {
        "src/*.py": {
            "memory": 1024,
            "maxDuration": 10
        }
    },
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/src/app.py"
      }
    ],
    "installCommand": "pip install -r requirements.txt"
  }