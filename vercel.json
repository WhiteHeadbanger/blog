{
    "functions": {
        "api/*.py": {
            "memory": 1024,
            "maxDuration": 10
        }
    },
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/api/app.py"
      }
    ],
    "installCommand": "pip install -r requirements.txt"
  }